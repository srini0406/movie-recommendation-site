[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = [
  "python manage.py collectstatic --noinput",
  "python manage.py migrate",
  "python regenerate_matrix.py"
]

[start]
cmd = "gunicorn movie_recommendation.wsgi:application --workers 2 --timeout 120"
